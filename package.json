{
  "name": "awcli",
  "version": "0.1.0",
  "description": "Run TypeScript agentic workflows from a global install",
  "type": "module",
  "bin": {
    "awcli": "dist/main.js"
  },
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "prepare": "npm run build",
    "check:contract": "tsc --noEmit",
    "build": "npm run check:contract && tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify:global": "bash scripts/verify-global-install.sh",
    "check": "npm run format:check && npm run typecheck && npm run test && npm run build",
    "check:gates": "npm run verify:typecheck-gate && npm run verify:contract-gate && npm run verify:spec-invariants",
    "verify:typecheck-gate": "bash scripts/verify-typecheck-gate.sh",
    "verify:contract-gate": "bash scripts/verify-contract-gate.sh",
    "verify:spec-invariants": "bash scripts/verify-spec-invariants.sh"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.4.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  }
}
